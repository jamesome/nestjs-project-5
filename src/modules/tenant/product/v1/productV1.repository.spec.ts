import { ProductV1Repository } from './productV1.repository';

describe('ProductV1Repository', () => {
  it('should be defined', () => {
    expect(new ProductV1Repository()).toBeDefined();
  });
});
