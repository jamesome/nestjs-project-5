import { TimestampColumnsHelper } from './timestamp-columns.helper';

describe('TimestampColumnsHelper', () => {
  it('should be defined', () => {
    expect(new TimestampColumnsHelper()).toBeDefined();
  });
});
